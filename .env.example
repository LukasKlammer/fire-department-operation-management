# Local-only Cypress Google OAuth test credentials.
# Copy to .env and fill these values only for authorized test accounts.
# Never commit .env or cypress.env.json.
CYPRESS_GOOGLE_CLIENT_ID=
CYPRESS_GOOGLE_CLIENT_SECRET=
CYPRESS_GOOGLE_REFRESH_TOKEN=
